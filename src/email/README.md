# Email assets

PNG signature blocks and lockups for email clients (SVG support is unreliable in mail).

Drop masters here after Penpot export frames exist. Until then, use wordmark `compact` PNGs from `dist/logo/wordmark/compact/`.
